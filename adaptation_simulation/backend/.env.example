# 环境变量示例文件
# Railway会自动设置PORT，本地开发时可以手动设置

# 服务器配置
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=production

# CORS配置（生产环境中应该设置具体的前端域名）
ALLOWED_ORIGINS=*

# 数据库配置（如果需要）
# DATABASE_URL=postgresql://user:password@host:port/database

# 其他配置
DEBUG=false
