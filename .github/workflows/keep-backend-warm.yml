name: Keep Render backend warm

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend /ping endpoint
        run: curl -sf --max-time 60 https://adaptation-sim-backend.onrender.com/ping
